---
apiVersion: v1
kind: Secret
metadata:
  name: sal-server
  namespace: sal
type: Opaque
data:
  # Base64 encode values for each item below
  # ex:  echo -n "admin_pass" | base64
  # ex: ADMIN_PASS: YWRtaW5fcGFzcw==
  ADMIN_PASS:
  DB_HOST:
  DB_USER:
  DB_PASS: